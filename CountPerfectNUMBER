class Solution {
    static int countSquares(int n) {
        int count = 0;
        int i=1;

        while(i<=n)
        {
            if((i*i)<n)
            count++;

            else
            break;

            i++;
        }

        return count;
        
    }
}
